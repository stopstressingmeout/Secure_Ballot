export default function VoteLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return (
      <section>
        <h1>Vote Layout</h1>
  
        {children}
      </section>
    );
  }